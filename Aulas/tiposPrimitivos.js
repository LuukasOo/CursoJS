const nome = 'João'; //STRING
const nome1 = 'Lucas'; //STRING
const nome2 = `Maciel`; //STRING
const num1 = 10; //NUMBER
const num2 = 20; //NUMBER
let nomeAluno; // undefined -> nao aponta pra lugar nenhum
let sobreNomeAluno = null;
const aprovado = true; //Boolean = TRUE OU FALSE (logico) 

//TypeOf mostrar tipo da variavel
console.log(typeof nome, nome);
console.log(typeof num1, num1);
console.log(typeof nomeAluno, sobreNomeAluno);
